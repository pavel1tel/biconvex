import { useUnit } from 'effector-react';

/**
 * @deprecated Use `useUnit` from 'effector-react` instead
 */

function useQuery(query) {
  const [data, stale, error, pending, start] = useUnit([query.$data, query.$stale, query.$error, query.$pending, query.start]);
  return {
    data,
    stale,
    error,
    pending,
    start
  };
}

/**
 * @deprecated Use `useUnit` from 'effector-react` instead
 */

function useMutation(mutation) {
  const [start, pending] = useUnit([mutation.start, mutation.$pending]);
  return {
    start,
    pending
  };
}

export { useMutation, useQuery };
