'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var effectorReact = require('effector-react');

/**
 * @deprecated Use `useUnit` from 'effector-react` instead
 */

function useQuery(query) {
  const [data, stale, error, pending, start] = effectorReact.useUnit([query.$data, query.$stale, query.$error, query.$pending, query.start]);
  return {
    data,
    stale,
    error,
    pending,
    start
  };
}

/**
 * @deprecated Use `useUnit` from 'effector-react` instead
 */

function useMutation(mutation) {
  const [start, pending] = effectorReact.useUnit([mutation.start, mutation.$pending]);
  return {
    start,
    pending
  };
}

exports.useMutation = useMutation;
exports.useQuery = useQuery;
